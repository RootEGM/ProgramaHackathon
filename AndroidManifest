<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackathon.medicaleasy.medicaleasy" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MedicalEasy"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Diabetes" android:label="Diabetes">
        </activity>
        <activity android:name=".CancerProstata" android:label="Prostata">
        </activity>
        <activity android:name=".hipertension" android:label="Diabetes">
        </activity>
        <activity android:name=".gastritis" android:label="Diabetes">
        </activity>
        <activity android:name=".Diabetes_Contacto" android:label="DiabetesContacto">
        </activity>
    </application>

</manifest>
